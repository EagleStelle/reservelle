// Production defaults. Replaced by environment.development.ts in dev builds.
export const environment = {
  production: true,
  // API prefix. Prepend a host if the backend is served separately in prod.
  apiUrl: '/api',
  backendUrl: '/',
};
